# Streamer SkeletonFX
## Локальный запуск
1. Клонировать репозиторий и перейти в папку:
   ```bash
   git clone <url-репо>
   cd streamer-skeletonfx
Установить зависимости:

npm install
Запустить dev-сервер:

npm run dev
Сайт откроется по адресу из вывода (http://localhost:8080).
